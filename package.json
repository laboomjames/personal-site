{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A lightweight static site generator that converts Markdown to HTML, with a blog and simple pages.",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "dev:build": "node build.js --dev",
    "dev:serve": "http-server docs -p 3000 -c-1",
    "dev": "npm run dev:build && npm run dev:serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^11.2.0",
    "marked": "^12.0.0"
  }
}
